name: Code Review (Go Lint + Test)

on:
  pull_request:
    paths:
      - '**'
      - 'examples/**'
      - '.github/workflows/**'
  push:
    branches: [main, master]
    paths:
      - '**'
      - 'examples/**'
      - '.github/workflows/**'

jobs:
  review:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true
      - name: Format Check
        run: |
          out=$(gofmt -l .)
          if [ -n "$out" ]; then
            echo "$out"
            exit 1
          fi
      - name: Vet
        run: go vet ./...
      - name: Unit Tests
        run: go test ./...
