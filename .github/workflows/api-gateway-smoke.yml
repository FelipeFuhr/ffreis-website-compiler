name: API Gateway Smoke

on:
  workflow_dispatch:
  schedule:
    - cron: '15 4 * * *'

jobs:
  smoke:
    uses: FelipeFuhr/ffreis-workflows-general/.github/workflows/general-http-smoke.yml@main
    with:
      paths: |-
        /
        /index.html
    secrets:
      SMOKE_BASE_URL: ${{ secrets.SMOKE_BASE_URL }}
